// Stub for Task 8 — Task 11 replaces this with the real composition
// (DeviceList/Send/Received/Settings tabs). Kept here only so app.js's
// import resolves and root routing can be exercised end to end.
export function MainView () { return null }
